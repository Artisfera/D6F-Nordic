#include <zephyr/dt-bindings/pinctrl/nrf-pinctrl.h>

&pinctrl {
    pwm0_motor_pins: pwm0_motor_pins {
        group1 {
            psels = <NRF_PSEL(PWM_OUT0, 1, 4)>;
        };
    };
};

&pwm0 {
    pinctrl-0 = <&pwm0_motor_pins>;
};
